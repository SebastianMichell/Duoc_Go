from django.urls import path
from . import views

urlpatterns = [
    # Más adelante agregaremos rutas reales si quieres una vista para productos
    # Por ahora basta con esto para que no dé error
]
